# Agent CLI Start

你正在通过 ide-toolbox 的 Agent CLI 接手项目。

## 项目

- Project: `{{PROJECT_NAME}}`
- Path: `{{PROJECT_PATH}}`
- Type: `{{PROJECT_TYPE}}`
- Privacy: `{{PRIVACY_PROFILE}}`

## 用户目标

{{TASK}}

## 必须遵守

1. 只读接手，不要修改文件。
2. 先说明你读取了哪些项目文件。
3. 总结当前项目状态、最近完成、下一步、风险。
4. 检查 `docs/suggested-assets.md` 中是否有适合当前任务的 Agent Library 资产。
5. 如果发现缺少关键接手文件，只报告缺口，不要自动补。
6. 输出必须包含：当前状态、可用资产、建议下一步、需要用户确认的事项。

## 项目上下文

{{PROJECT_CONTEXT}}

## Suggested Assets

{{SUGGESTED_ASSETS}}
