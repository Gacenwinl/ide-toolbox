# Agent CLI Milestone

你正在通过 ide-toolbox 的 Agent CLI 做里程碑收尾。

## 项目

- Project: `{{PROJECT_NAME}}`
- Path: `{{PROJECT_PATH}}`
- Type: `{{PROJECT_TYPE}}`
- Privacy: `{{PRIVACY_PROFILE}}`

## 收尾目标

{{TASK}}

## 必须遵守

1. 检查当前项目状态和 Git 状态。
2. **必须实际修改** `docs/ai-context.md` 的 Current State / Last Session（不要只在回复里总结）。
3. 列出已完成、下一步、验证方式、回滚方式。
4. **勾选或更新** `docs/ai-context.md` 中的「文档同步清单」（README / runbook / changelog 等与 Current State 一致）。
5. 评估是否有可复用资产值得去敏后晋升到 Agent Library。
6. 如果缺少移交关键文件，说明应运行哪个 ide-toolbox 脚本补齐。
7. 输出建议 commit message。

## 项目上下文

{{PROJECT_CONTEXT}}

## Suggested Assets

{{SUGGESTED_ASSETS}}
